# Relay Node Query Stitching
## This is temporary solution for stitching Relay-compatible services node query using graphql-tools

### Getting started
```
  cd post && yarn && yarn start
  cd product && yarn && yarn start
  cd gateway && yarn && yarn start
```